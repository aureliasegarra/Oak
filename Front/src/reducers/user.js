const initialState = {
  email: 'marina@oak.fr',
  password: 'oak',
  username: 'marina',
};

export default (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};

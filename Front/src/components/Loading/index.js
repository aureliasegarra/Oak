// == Import npm
import React from 'react';

import './styles.scss';

// == Composant
const Loading = () => (
  <div className="loading-text">
    <p>Loading...</p>
  </div>
);

// == Export
export default Loading;
